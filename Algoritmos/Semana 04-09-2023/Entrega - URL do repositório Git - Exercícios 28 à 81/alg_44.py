import math as m
num = float(input("\nEntre com o logaritmando: "))
base = float(input("\nEntre com a base: "))
logaritmo = (m.log(num) / m.log(base))
print("\nO logaritmo de", num, "na base", base, "é:", logaritmo)
print("\n")